version: 2

sources:
  - name: stripe # source name that I provide
    database: dbt-tutorial # equivalent of project name in Bigquery
    schema: stripe # provide schema/dataset name
    tables:
      - name: payment #table name that we are sourcing data from 
        loaded_at_field: _batched_at
        freshness:
          warn_after: {count: 12, period: hour}
          error_after: {count: 24, period: hour}
      